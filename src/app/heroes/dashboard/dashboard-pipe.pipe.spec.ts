import { DashboardPipePipe } from './dashboard-pipe.pipe';

describe('DashboardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
